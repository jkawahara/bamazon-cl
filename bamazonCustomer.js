// *** REQUIRED MODULES: mysql, inquirer, console.table


// *** DECLARE GLOBAL VARIABLES / FUNCTIONS
// Create connection to MySQL bamazon DB with connection options: defining host, port, user, password, database

// Display item_id, product_name, price viewCustomer()
  // console.table()

// First, prompt customer for item_id to order promptCustomerMsg1()
  // inquirer.prompt().then(answers => {})

// Second, prompt customer for number of units to order promptCustomerMsg2()
  // inquirer.prompt().then(answers => {})


// *** MAIN CONTROLLER

// Connect to MySQL bamazon DB
  // if(err) throw err

  // viewCustomer()
  
  // promptCustomerMsg1()
  
  // promptCustomerMsg2()

  // if (stock_quantity < units)
    // Log 'Insufficient quantity!'
    // End order
  // else
    // stock-quantity -= units
    // Log order cost
  
  // viewCustomer()